const express = require('express')
const router = express.Router()

// STUB — no routes defined yet. Do not call from frontend.
// User profile routes will be implemented in Phase 6.

// GET /api/users/:id
// PUT /api/users/:id

module.exports = router