const express = require('express')
const router = express.Router()

// STUB — no routes defined yet. Do not call from frontend.
// Admin routes will be implemented in Phase 5.

// All admin routes go here
// Protected by auth and the adminOnly middleware

module.exports = router