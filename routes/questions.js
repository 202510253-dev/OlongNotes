const express = require('express')
const router = express.Router()

// STUB — no routes defined yet. Do not call from frontend.
// Q&A routes will be implemented in Phase 4 (next phase).

// GET /api/questions
// POST /api/questions
// GET /api/questions/:id
// DELETE /api/questions/:id

module.exports = router